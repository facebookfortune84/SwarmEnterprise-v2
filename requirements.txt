# SwarmOS Root Requirements
# Pinned where stability matters, relaxed where the ecosystem is volatile

# Core Framework (safe to pin)
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.13.4
pydantic-settings==2.1.0

# LLM & AI (let pip resolve around crewai/embedchain)
crewai==0.28.8
langchain
langchain-community
langchain-openai

# Database (let pip pick a version compatible with langchain/embedchain)
sqlalchemy
aiosqlite==0.19.0

# Vector DB (let pip pick a version compatible with embedchain)
chromadb

# Utilities
python-dotenv==1.0.0
requests==2.33.0
stripe==6.7.0
PyJWT==2.8.0
jinja2==3.1.2
psutil==5.9.8

# Async
httpx==0.25.0
aiofiles==23.2.1

# Development (safe to pin)
pytest==9.0.3
pytest-asyncio==0.21.1
black==26.3.1
ruff==0.1.7

# Observability
python-json-logger==2.0.7
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp==1.22.0

# Queue and async
redis==5.0.0
celery==5.3.1

# Prometheus metrics
prometheus-client==0.16.0

# Sentry
sentry-sdk==2.8.0

# Security
pip-audit==2.10.0
reportlab==4.0.0
